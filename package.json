{
  "name": "markdown-docs",
  "version": "1.0.0",
  "description": "Markdown documentation with Prettier formatting and linting",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write 'docs/**/*.md'",
    "format:check": "prettier --check 'docs/**/*.md'",
    "lint": "markdownlint 'docs/**/*.md' && textlint 'docs/**/*.md'",
    "lint:fix": "markdownlint --fix 'docs/**/*.md' && textlint --fix 'docs/**/*.md'",
    "postinstall": "simple-git-hooks"
  },
  "simple-git-hooks": {
    "pre-commit": "npm run lint"
  },
  "keywords": [
    "markdown",
    "documentation",
    "prettier"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "markdownlint-cli": "^0.48.0",
    "prettier": "^3.8.3",
    "prettier-plugin-organize-imports": "^4.3.0",
    "simple-git-hooks": "^2.13.1",
    "textlint": "^15.7.1",
    "textlint-rule-preset-ja-technical-writing": "^12.0.2"
  }
}
